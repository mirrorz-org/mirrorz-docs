# MirrorZ Docs Preview

A simpler preview for mirrorz-docs contributors.

```bash
python3 -m http.server 8000
```

And then open in browser: `http://127.0.0.1:8000/.preview/`

Note that this is for debug & preview only -- not for production.
Also note that most of the code are generated by LLM.
